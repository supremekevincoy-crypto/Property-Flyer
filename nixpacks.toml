[phases.setup]
nixPkgs = ["python311", "pango", "cairo", "gdk-pixbuf", "libffi", "shared-mime-info", "harfbuzz", "fontconfig", "freetype", "gobject-introspection"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
